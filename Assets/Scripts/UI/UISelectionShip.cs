using UnityEngine;

namespace StellarArmada.UI
{
    // Local player representation of ships in custom selection groups
    // Instantiated by the UI Ship Factory
    // Interacted with in the player's match menu
    public class UISelectionShip : MonoBehaviour
    {
        public uint entityId = 0;
    }
}